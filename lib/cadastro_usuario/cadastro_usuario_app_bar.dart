import 'package:flutter/material.dart';

AppBar cadastroUsuarioAppBar({String title, nav}) {
  return AppBar(
      title: Text(title),
      backgroundColor: Color.fromARGB(255, 240, 152, 0)
    );
}
